# CircleCI configuration file
version: 2.1

jobs:
  build:
  working_directory: ~/circleci-python
    docker:
     - image: "circleci/python:3.6.4"
    steps:
      - checkout
      -run: python3 main.py

workflows:
    #Store manager
    store_manager:
      jobs:
        - build
      on:
        branches:
          - master



